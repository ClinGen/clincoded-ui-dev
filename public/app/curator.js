'use strict';

var React = require('react');

var Curator = module.exports.Curator = React.createClass({
    render: function() {
        return (
            <div>Curator Page</div>
        );
    }
});
